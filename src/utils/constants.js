export const paging = {
    PAGE_SIZE: 10
}

export const ordering = {
    directions: {
        ASCENDING: 'asc',
        DESCENDING: 'desc'
    }
}

export const query = {
    HISTORY_SIZE: 10
}

export const NETWORK = {
    RETRIES: 3,
    TIMEOUT: 2000
}